# Base layer: shipped with the app and needed before first-run setup.
# Heavy components (PyTorch, GigaAM, ffmpeg, models) are installed by the
# in-app Setup screen, see core/setup_plan.py.
flask>=3.1
python-dotenv>=1.0
pywebview>=5.0
sounddevice>=0.5
soundcard>=0.4
numpy>=1.24
uv>=0.4
pypdf>=5          # reading PDF transcripts and documents
# macOS: system-audio capture in the desktop app (Core Audio process taps)
pyobjc-framework-CoreAudio>=11; sys_platform == "darwin"
